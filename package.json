{
  "name": "balance-budget",
  "private": true,
  "packageManager": "pnpm@10.34.1",
  "scripts": {
    "dev": "pnpm --filter balance dev",
    "build": "pnpm --filter balance build",
    "lint": "pnpm --filter balance lint",
    "test": "pnpm --filter balance test",
    "typecheck": "pnpm --filter balance typecheck",
    "format": "pnpm --filter balance format",
    "format:check": "pnpm --filter balance format:check",
    "codegen": "pnpm --filter balance codegen",
    "sandcastle": "pnpm dlx tsx .sandcastle/main.mts"
  },
  "devDependencies": {
    "@ai-hero/sandcastle": "^0.7.0"
  }
}
